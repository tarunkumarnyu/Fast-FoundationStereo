__pycache__
*.vscode
cfg.yaml
*.pth
*.pkl
*.pyc
*.so
*.egg-info
# Ignore weight files but keep the folder structure
weights/*
!weights/.gitkeep
output/
.claude/
# TRT engines are hardware-specific — rebuild per device
engines/**/*.engine
# ONNX data files (large)
engines/**/*.onnx.data