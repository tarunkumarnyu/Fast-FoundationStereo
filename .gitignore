__pycache__
*.vscode
cfg.yaml
*.pth
*.pkl
*.pyc
*.so
*.egg-info
# Ignore weight files but keep the folder structure
weights/*
!weights/.gitkeep
output/
.claude/
# TRT engines are hardware-specific — rebuild per device
engines/**/*.engine
# ONNX files (large, regenerate with make_onnx.py)
engines/**/*.onnx
engines/**/*.onnx.datacpp/log/
cpp/ffs_cpp.log
